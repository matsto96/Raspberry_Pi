import fft_plot as FFT_f
import matplotlib.pyplot as plt

print("Starting FFT analytics")

#Load data from file or give file as input to function

myFFT = FFT_f fft_of_data(data)

# Make plot of myFFT

plt.plot(myFFT)
plt.show()